{
  "name": "ghostlight",
  "version": "0.1.0",
  "private": true,
  "description": "Open source 3D scanning for the original Xbox Kinect. Runs on modern GPUs.",
  "type": "module",
  "scripts": {
    "start": "npm run build && python ghostlight.py",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "python ghostlight.py --no-open"
  },
  "dependencies": {
    "three": "^0.185.1",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "vite": "^6.0.7"
  }
}
